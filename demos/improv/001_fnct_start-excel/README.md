# Exercise 1 - given a .csv , we want a command to launch EXCEL even if the filename does not exist



We can use `start  file.csv` but that will fail if the file does not already exist.



We want a function `start-excel` when shows the development over stages in [implementation file: start-excel.ps1](./start-excel.ps1)



Test file [Sample test code](./testing.ps1) is also provided.

